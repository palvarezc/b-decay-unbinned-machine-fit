Optimise hyperparameters/optimiser
    ODE params + dict
    Combine scripts
Cleanup optimizer scripts (combine + pass params as int?)
Default to randomised variables
Change to proper sample size.

Cleanup likelihood scripts/Move GPU statements
Do ensembles (parallel?) & plot distributions.

Turn coeff file into classes?
Improve README/docs (pycharm, tf warnings, move doc strings, integral accuracy)

Add mass terms
Add S-wave
Add background